General
    - Crashes X server when using inel driver? NVidia, and Xephyr, seem
      to be ok.
    - Opacity settings
    - sanitizeSize ??

Scrollbars
    - drawing of slider handle *over* buttons when at min/max not done

ListView headers
    - Need way to detect first header. At the moment, just draw splitter
      on right hand side.
    - Drawing of lines - custom GC
    - Detection of sorted column

Combo Boxes
    - Unified style not working
    - Editable has double arrows
    - Editable's list is not drawn 'view' (i.e. KDE) like

Group Boxes
    - Alignment of group box titles. Currently if use Gtk2 code, then text
      is clipped on left. :-(

Popup-menus
    - Rounding not working

Tooltips
    - Rounding not working

XProperty
    - Can't set, as seems to crash at the moment.

WindowDrag
    - Gtk3 should now support this.

Progress bars
    - Orientation
    - Animation

Toolbars
    - Orientation

Menubars
    - Detecting active
    - Items seem to only be rounded on top even on mouse-over
